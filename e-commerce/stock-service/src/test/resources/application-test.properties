# Configuration de test pour le service stock

# Base de données de test (H2 en mémoire)
spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# JPA Configuration pour les tests
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.H2Dialect
spring.jpa.properties.hibernate.format_sql=true

# Console H2 pour les tests
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# Désactiver Eureka pour les tests
eureka.client.enabled=false

# Logging pour les tests
logging.level.com.example.ecommerce.stockservice=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# Configuration des tests
spring.test.database.replace=none 